    <script type='text/javascript'>//<![CDATA[ 

        function Font(data) {
            var self = this;
            self.id = ko.observable(data._id ? data._id : "");
            self.name = ko.observable(data.name ? data.name : "New Font");
            self.css = ko.observable(data.css ? data.css : "");
            self.template_location = ko.observable(data.template_location ? data.template_location : "");
            self.css = ko.observable(data.css ? data.css : "");
            self.link_data = ko.observable(data.link ? data.link : "");
            self.link_data_clean = ko.observable(data.link ? data.link.match(/href=[\"\'][^\"\'']*/)[0].match(/http.*/)[0] : "");

            self.new_font = ko.computed(function(){
            	return self.id() != "";
            });

        }

    //]]>  
    </script>